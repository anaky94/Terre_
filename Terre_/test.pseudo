si variable >= 0 et variable <= 5
  afficher("variable est comprise entre 0 et 5")

si variable < 5 et non(variable < 0)
  afficher("variable est strictement inférieure à 5 mais jamais négative")

condition = 42
tantque (condition = 42)
  afficher("la condition est vraie")
  condition = condition + 1

condition = 41
repeter
  afficher("la condition est vraie")
  condition = condition + 1
tantque (condition = 42)

pour i de 0 a 4
  afficher(i)

pour i de 0 a 4 par pas de 2
  afficher(i)

fonction direBonjour(prenom)
  afficher("bonjour " + prenom)

fonction somme(nombre1, nombre2)
  resultat = nombre1 + nombre2
  retourner resultat

lire(age)
  si age < 18
    afficher("Vous n'êtes pas majeur")
  sinon
    afficher("Vous êtes majeur")
  
/* *TROOUVER LA SUISSE 
Créez un programme qui prend en paramètre trois entiers et affiche la valeur du milieu.


Exemples d’utilisation :
$> ruby exo.rb 11 40 34
34

$> ruby exo.rb 2 1 3
2

$> ruby exo.rb 2 2 2
erreur.

Fonctions interdites: 
-La fonction sort

*/

soit 3 valeur 

tableau = [1,2,3]

// pour i de 1 a tableau.longeur
si argument1 > arguments2 || argument1 > argument3 
  return argument1


  
si argument2 > arguments1 || argument2 > argument3 
  return argument2
si argument3 > arguments1 || argument3 > argument2 
  return argument3


  